# Публикация 0.3.0

Публикация репозитория и готового приложения на GitHub разрешена владельцем. Этот список помогает проверить содержимое и точный выпуск; он не вводит нового согласования.

## Исходники

- Использовать [проверенный список файлов](source-publication-files.txt) и [отчёт аудита](source-publication-audit-2026-09-16.md). Перед commit сопоставить с ним staged-файлы: `.gitignore` не исключает уже отслеживаемые файлы.
- В публикацию входят Swift/Python source, синтетические тесты, scripts, Resources, документация и workflow. Включение исходника legacy-коннектора не означает включение его Python runtime в обычное приложение.
- `build/`, `.venv/`, личные экспорты, runtime caches, WebKit storage, credentials и локальная конфигурация инструментов исключены.
- Лицензия собственных исходников не выбрана. **Владелец разрешил публикацию без LICENSE**; выбирать условия от его имени или называть проект распространяемым по неуказанной лицензии не нужно. Это не блокирует разрешённую публикацию. Уведомления о реально включённых сторонних зависимостях сохраняются.

## Пакет 0.3.0 build 9

1. Зафиксировать проверки нового обычного окна, Dock, разделов и открытия нужного профиля из системного виджета в [заметках к выпуску](releases/0.3.0.md).
2. Проверить точный bundle: версия 0.3.0 / build 9, совпадающая архитектура executable и виджета, подписи и системные зависимости. Обычный пакет не содержит Connector или Python runtime.
3. Проверить `GarminDesk-0.3.0-arm64.zip`, `.dmg` и `-SHA256.txt`; сопоставить executable из архива с установленной проверенной копией. Исключить пользовательские данные и абсолютные developer home paths из бинарников.
4. Публиковать именно проверенные файлы. Указать фактически проверенные систему/архитектуру, отсутствие Developer ID/notarization и стандартный первый запуск через macOS Open Anyway при необходимости.

[Проверки 0.2.0 build 7](validation.md) остаются историей. Они не подтверждают интерфейс или пакеты 0.3.0. Intel, отдельный чистый Mac и другие версии macOS не заявляются проверенными без фактического результата. Аудит workflow и его реальный запуск выполняются отдельно.
