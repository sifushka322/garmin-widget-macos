# GarminDesk 0.5.0 publication record

Version: v0.5.0
Build: 11
Candidate commit: cf6bd8d433c4f586468520bcd544866781120bb5
Overall result: NOT RUN
Normal upgrade: NOT RUN
Owner override: explicit release request after disclosure
Override reason: Published at the owner’s explicit request after disclosure that normal system-widget upgrade validation remains unverified; desktop/gallery automation was unavailable.
Unverified coverage: Normal replacement of 0.4.0 with 0.5.0 while desktop widgets remain installed; system-gallery icon and all five kinds in three sizes; existing and new live widget instances, refresh delays, settings migration, and widget links after that replacement. Live Garmin sign-in and Intel GUI upgrade were not tested.
Service intervention before completing checks: none

## Scope and decision

The owner explicitly requested publication after being told that normal system-widget upgrade validation was still outstanding, then specified `main` and publication through the GitHub plugin. This is a documented exception to the standard [upgrade procedure](../../widget-upgrade-validation.md), not a passing upgrade report.

The observed local baseline was macOS 26.6.2 (25G83), Apple Silicon, with GarminDesk 0.4.0 installed. Desktop/gallery automation could not operate the required system surface. The candidate was not installed and a normal upgrade was not performed. No cache clearing, extension registration, service termination, logout, or reboot was used to manufacture a passing result. No personal readings, account data, or screenshots are included in this report.

## Automated release evidence

The exact candidate passed [release CI](https://github.com/sifushka322/garmin-widget-macos/actions/runs/35105278274) on September 16, 2026. Version validation, JavaScript/Python contracts, both native architecture jobs, and verified draft preparation all completed successfully. The native jobs passed their full assertion suites and visual fixtures, sandbox-sharing checks, release builds, host/extension signatures and versions, and DMG/ZIP packaging. The draft contains exactly six uploaded package/checksum files for Apple Silicon and Intel, and its target is the candidate commit above.

The first run reached the 30-minute Intel job limit after passing its functional assertions while generating app images. Investigation found that the command-line screenshot fixture retained closed hosting views. Per-frame cleanup changed retained views from 24 to zero and peak memory from 472 MiB to 102 MiB on an identical 24-frame local matrix; all PNGs remained byte-for-byte identical. The final candidate also avoids repeatedly compiling unused renderer bodies for data-only tests, preserves full rendering coverage, and allows a 60-minute job budget. Application, connector, and bundled resource contents are unchanged by these CI repairs.

The [publication approval](../approvals/v0.5.0.json) binds the exact candidate, successful run, six package/checksum files, this report, and the public release notes. The promotion workflow independently compares those files with the two architecture artifacts from that run before publishing the same draft. Automated checks and matching files do not establish normal GUI-upgrade acceptance.
